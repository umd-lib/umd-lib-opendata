# Core dependencies for example Python scripts in static/code/

# OAI-PMH protocol client for metadata harvesting
pyoai==2.5.0

# RDF and JSON-LD processing for semantic data
rdflib==7.6.0

# HTTP client for API requests
requests==2.32.3

# SRU (Search/Retrieve via URL) protocol client
sru-queryer==2.1.3

# Transitive dependencies (auto-installed, but pinned for reproducibility)
lxml==4.9.4
certifi==2024.7.4
charset-normalizer==3.3.2
idna==3.7
urllib3==2.2.2
setuptools==70.2.0
six==1.16.0
